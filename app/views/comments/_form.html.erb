<div class="modal-dialog">
  <div class="modal-content">
    <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal">&times;</button>
      <h4 class="modal-title">Article comment</h4>
    </div>
    <%= form_for(@comment, :issue_id=> params[:issue_id], :magazine_id=> params[:magazine_id]) do |f| %>
    <div class="modal-body">
          <% if @comment.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

                <ul>
                  <% @comment.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
          <% end %>
          <div class="field">
            <%= f.label :description %><br>
            <%= f.text_area :description, size: "70×50" %>
          </div>

    </div>
    <div class="modal-footer">
      <a href="#" class="btn btn-white" data-dismiss="modal">Close</a>
      <%= f.hidden_field :id, :value => params[:id] %>
      <%= f.hidden_field :issue_id, :value => params[:issue_id] %>
      <%= f.hidden_field :magazine_id, :value => params[:magazine_id] %>
      <%= f.submit :class=> "btn btn-primary" %>
    </div>
   <% end %>
  </div><!-- /.modal-content -->
</div><!-- /.modal-dialog -->


